# vim: ft=gitconfig
[user]
    name = alexis-moins
    email = sinus.nacre0v@icloud.com

[init]
    defaultBranch = main

[core]
    editor = nvim
    pager = delta

[delta]
    tabs = 6
    side-by-side = true
    true-color = always

    navigate = true
    file-modified-label = "~"

    syntax-theme = Rose-Pine-Moon

    plus-style = syntax "#3b4456"
    minus-style = syntax "#4b3148"

    line-numbers-plus-style = "#9ccfd8"
    line-numbers-minus-style = "#eb6f92"

    plus-emph-style = "#9ccfd8" ul "#3b4456"
    minus-emph-style = "#eb6f92" ul "#4b3148"

[diff]
    colorMoved = default
    mnemonicPrefix = true

[interactive]
    diffFilter = delta --color-only

[color "diff"]
    meta = bold black

[difftool]
    prompt = true

[merge]
    conflictstyle = diff3

[mergetool]
    prompt = false
    keepBackup = false

[rerere]
    enabled = true

[alias]
    ci = commit
    amend = commit --amend --no-edit

    unstage = restore --staged --
    discard = checkout --

    untracked = ls-files --others --exclude-standard

    fap = fetch --all --prune
    current = rev-parse --abbrev-ref HEAD

    bl = branch --format '%(color:red)[%(HEAD)]%(color:white) %(refname:short) %(color:green)%(upstream:short) %(color:blue)%(authorname) %(color:bold black)%(committerdate:relative) %(upstream:track)'
    blr = branch --remote --format '%(color:red)[%(HEAD)]%(color:white) %(refname:short) %(color:blue)%(authorname) %(color:bold black)%(committerdate:relative)'
    rename = branch --move

    uncommit = reset HEAD^
    remove-commit = reset --hard HEAD^

    diff-staged = diff --staged
    diff-unstaged = diff --diff-filter=M

    precommit = diff --staged --diff-algorithm=minimal --ignore-all-space

    l = log --all --graph -n 15
    ll = log --all --graph

    graph = log --graph -10 --branches --remotes --tags  --format=format:'%Cgreen%h %Creset• %<(75,trunc)%s (%cN, %cr) %Cred%d' --date-order

    ignore = !git rm -r --cached . && git add --all

[format]
    pretty = format:%C(auto)%h [%D] %C(brightblue)%an%C(reset) %s %C(bold black)%cr%C(reset)

[push]
    autoSetupRemote = true
    default = "current"

[remote "origin"]
    prune = true

[pull]
    rebase = true
