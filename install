#!/usr/bin/env bash

set -e

[[ ! -d "${HOME}/.config" ]] && mkdir -v "${HOME}/.config"
[[ ! -d "${HOME}/projects" ]] && mkdir -v "${HOME}/projects"

brew bundle install

fish utils/packer-install

stow --delete . && stow --stow --verbose .

pipx install httpx
pipx install pre-commit
