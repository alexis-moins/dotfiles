# Author: Alexis Moins
# Creation: 28 aoû 2021
# vim: ft=zsh syn=zsh

# Prevent the python venv module from displaying virtual env in the prompt
export VIRTUAL_ENV_DISABLE_PROMPT=1

# Add the pyenv shims directory to the path
path=( "${HOME}/.pyenv/shims" ${path} )

# Enable completion for pyenv
eval "$(pyenv init -)"

# Create a new python virtual environment
alias venv='python -m venv'

# Activate an existing python virtual environment
alias activate='source **/bin/activate'
