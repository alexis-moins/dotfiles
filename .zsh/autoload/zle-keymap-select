# Change cursor shape based on vi mode
# Author: Alexis Moins
# Creation: 17 janvier 2020
# vim: ft=zsh syn=zsh

case ${KEYMAP} in
    (vicmd) 
        # Command mode
        RPROMPT="%F{10} -- COMMAND -- %f" ;;
    (viins|main)
        # Insert | Normal mode
        RPROMPT="" ;;
esac

# Refresh prompt
zle reset-prompt
