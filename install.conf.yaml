- defaults:
    link:
      relink: true
      create: true
      glob: true

- clean: ['~', '~/.config']

- link:
    ~/.ssh/config:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.config: config/*
    ~/.zshenv:

- shell:
  - cp hyper.js ~/.hyper.js
  - git submodule update --init --recursive
