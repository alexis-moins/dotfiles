- defaults:
    link:
      relink: true
      create: true
      glob: true

- clean: ['~', '~/.config', '~/.vim', '~/.zsh']

- link:
    ~/.ssh/config: ssh/config
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.vim: vim/**
    ~/.config/bat: config/bat
    ~/.zshenv:
    ~/.zsh/config: zsh/config
    ~/.zsh: zsh/.*

- create:
  - ~/.zsh/cache
  - ~/.vim/bundle
  - ~/.vim/undo-history

- shell:
  - [vim +PlugInstall +qa, 🎉 Installing vim plugins]
