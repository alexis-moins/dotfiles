local dressing = require('dressing')

dressing.setup({

    input = {

        win_options = {
            -- No transparency
            winblend = 0,

            winhighlight = "NormalFloat:Normal,FloatBorder:Directory",
        }
    }

})
